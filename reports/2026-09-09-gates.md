# 三门运行记录（2026-09-09，M2.7–M2.9 落库、M3.1 身份与业务片、M3.2 薄 HTTP 面、M3.3 凭证四步之后）

- 命令：`uv run ruff check . && uv run ruff format --check .`、`uv run lint-imports`、`uv run pytest -q`、`uv run pytest --collect-only -q`
- 环境：本机 Windows 11，Redis db1 与 Postgres 测试库 aegis_test 均可达（docker compose），`AEGIS_FAKE_LLM=1`，全程零真实 LLM 调用；
  langchain 1.4.0 / langchain-core 1.6.1 / langgraph 1.2.11 / langgraph-checkpoint-postgres 3.1.2 / psycopg 3.3.5 / fastapi 0.141.1 / openai 3.7.0
- 口径：pytest 收集项数含跳过项，不设目标值；跳过为 0 表示两处外部依赖都在场
- 结果：ruff 与 format 通过（175 files already formatted）；import-linter 五契约 kept（含新增"business 只被入口引用"）；
  pytest **939 passed、0 skipped**；收集：939 tests collected
- 逐步实跑（同一会话，每步提交前）：M2.7 落库 769 / 5 skipped → M2.8 849 / 1 → M2.9 **862 / 0** → M3.1 **907 / 0** → M3.2 **936 / 0** → M3.3 **939 / 0**
- 突变抽查（各步一处，改后即红、复原即绿且与代码稿逐字节一致）：Approvals"任一非批准"判定取反 → 13 红；守卫 `worse()` 取更宽 → 24 红；
  ModelCall 半截 LLM 重放判定恒假 → 2 红；`fetch_owned_order` 删 user 归属比对 → 2 红；读面 `viewable_session` 删"终端用户只看本人"判定 → 1 红；
  chat 端点删等审批 409 闸 → 2 红（真 PG HTTP 端到端 + 路由测试各一）
