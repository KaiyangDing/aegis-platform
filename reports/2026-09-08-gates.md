# 三门运行记录（2026-09-08，M2.7 HITL 审批 / M2.8 守卫三段 / M2.9 崩溃恢复三步稿件齐备时）

- 命令：`uv run ruff check . && uv run ruff format --check .`、`uv run lint-imports`、`uv run pytest -q`、`uv run pytest --collect-only -q`
- 环境：本机 Windows 11，Redis db1 与 Postgres 测试库 aegis_test 均可达（docker compose），`AEGIS_FAKE_LLM=1`，全程零真实 LLM 调用；
  langchain 1.4.0 / langchain-core 1.6.1 / langgraph 1.2.11 / langgraph-checkpoint-postgres 3.1.2 / psycopg 3.3.5
- 口径：pytest 收集项数含跳过项，不设目标值；跳过为 0 表示两处外部依赖都在场且三步代码全部在树上
- 结果：ruff 与 format 通过（139 files already formatted）；import-linter 四契约 kept（含 gateway ↛ runtime）；
  pytest **862 passed、0 skipped**；收集：862 tests collected
- 逐步拷入（同一批测试文件、只换 app/ 树，`tests/engine/runtime` 子集）：M2.7 → 245 passed / 5 skipped（M2.8 / M2.9 守卫）；
  +M2.8 → 325 passed / 1 skipped（M2.9 守卫）；+M2.9 → 338 passed / 0 skipped
- 突变抽查（各步一处，改后即红、复原即绿）：Approvals 的"任一非批准"判定取反 → 13 红；守卫档位比较 `worse()` 取更宽 → 24 红；
  ModelCall 的重放判定改为恒假 → 2 红
