# 三门运行记录（2026-09-05）

- 命令：`uv run ruff check . && uv run ruff format --check .`、`uv run lint-imports`、`uv run pytest -q`、`uv run pytest --collect-only -q`
- 环境：本机，Redis db1 与 Postgres 测试库 aegis_test 均可达，`AEGIS_FAKE_LLM=1`
- 口径：pytest 收集项数含跳过项，不设目标值；跳过为 0 表示两处外部依赖都在场
- 结果：ruff 与 format 通过；import-linter 三契约 kept；pytest 467 passed、0 skipped；收集：467 tests collected in 1.78s
